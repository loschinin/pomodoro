import React from 'react';
import {hot} from 'react-hot-loader/root';

const _Header = () => {
    return (
        <div>
            <h1>
                Reddit for our own
            </h1>
            
        </div>
    );
};

export const Header = hot(_Header);